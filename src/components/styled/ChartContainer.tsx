import styled from "styled-components";
import MainContainer from "./MainContainer";

const ChartContainer = styled(MainContainer)`
    border-radius: 15px 15px 0 0;
    margin: 0 auto;
    padding: 15px 15px 5px 15px;
`;

export default ChartContainer;
